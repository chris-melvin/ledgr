# Implementation notes — spending-clarity

- Migrations 0019 (ledger_events) and 0020 (bucket flag + shortcut bucket) are written but NOT yet applied to Supabase. Apply via SQL editor before manual verification (task 8.2).
- Shared package `check-types` failures (TS2835 extension imports) are pre-existing on insights files; spending-stats.ts follows the same working convention.
- Web shortcuts live in localStorage (use-shortcuts.ts), not the DB `shortcuts` table; the DB `bucket_id` column is for future/mobile parity.
- Global lint baseline: 143 warnings on main (`--max-warnings 0` fails pre-change). Touched files have 0 errors.
